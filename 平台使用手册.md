# 文博链平台 · 使用手册（v1.0 附赠）

> 文件：区块链平台.html ｜ 形态：手机浏览器打开即 APP ｜ 离线可用，数据存本机

## 一、快速开始（3 步）

1. **双击打开** `区块链平台.html`（手机用户：把文件发到手机，用浏览器打开）；
2. **登录**：馆藏单位/检测机构/管理员输入账号密码（演示账号见登录页下方提示）；公众点「游客模式」免登录；管理员多一个「管理」页；
3. 点底部按钮操作：**首页 / 登记 / 溯源 / 授权 / 验证 / 链 / 我的**。

首次打开会自动生成"创世区块 + 2 件示例文物 + 1 条检测记录"，约需几秒（真的在挖矿）。

## 二、角色与权限

| 角色 | 能做什么 | 不能做什么 |
| --- | --- | --- |
| 馆藏单位（绵阳市博物馆） | 登记文物、记录事件、发布授权、验证、查链 | — |
| 检测机构（文物检测中心） | 记录检测/修复事件、验证、查链 | 登记文物、发布授权 |
| 公众（游客） | 验证真伪、浏览链 | 所有写入操作 |
| 平台管理员（平台运营方） | 全部写入功能 + 机构准入/停用 + 审计日志 | — |

## 三、各功能页说明

| 页 | 干什么 | 关键操作 |
| --- | --- | --- |
| 首页 | 总览 | 看统计数字和最新 3 个区块 |
| 登记 | 文物建立数字孪生并上链 | 填表 →（可选）📸 拍照取证，照片指纹随登记上链 →「登记并上链」→ 等挖矿动画结束 |
| 溯源 | 给文物加不可篡改的事件记录 | 选文物 → 选类型 → 填内容 → 上链 |
| 授权 | 数字资源授权给第三方 | 选文物 → 填被授权方/用途/期限 → 上链 |
| 验证 | 文件真伪核验 | 下载档案文件 → 改一个字 → 上传验证；「打印凭证」/「下载凭证文件」；复制凭证扫码串生成真码 |
| 扫一扫 | 扫码核验 | 相机对准凭证二维码自动核验（🔦 手电筒补光）；或手动输入凭证编号 |
| 链 | 链浏览器 | 看每个区块的哈希、nonce、确认数；「校验整条链」体检；「模拟 1 个节点被篡改」看共识；「导出区块数据」下载 JSON |
| 管理 | 管理员专属 | 机构准入/停用、账号管理（为机构开账号）、审计日志、平台概览 |
| 我的 | 设置 | 调挖矿难度、重置全部数据 |

## 四、把网页变成"手机 APP"

**安卓**：用 Chrome 打开 → 右上角菜单 →「添加到主屏幕」→ 桌面出现图标，点击即全屏打开。
**iPhone**：用 Safari 打开 → 分享按钮 →「添加到主屏幕」→ 桌面出现图标。

> 说明：本平台是单文件网页应用（PWA 式用法），不是原生 APK。它不需要安装、不需要应用商店，这也是演示阶段最快的方式；后续可打包成原生 APP（同样的界面与逻辑，包一层壳即可）。

## 五、技术说明（评委/技术方会问）

| 问题 | 答案 |
| --- | --- |
| 是真区块链吗？ | 是真实的区块链结构与算法：每个区块含前块哈希 + 工作量证明（SHA-256 挖矿，难度可调）+ 链式校验；数据持久化在本机 localStorage |
| 共识怎么做的？ | 平台展示 4 节点联盟模式：每个新区块出块后 4 节点逐一确认；「模拟节点篡改」演示多数共识——坏节点被隔离 |
| 审计怎么做？ | 每次登录、登记、溯源、授权、验证、机构操作都写入审计日志（仅管理员可见），全程留痕 |
| 能导出数据吗？ | 链浏览器可导出全部区块 JSON；管理员可导出审计日志；验证页可打印/下载文物存证凭证 |
| 怎么登录？ | 多账号体系：museum/123456（馆）、exam/123456（检测）、admin/admin888（管理员），公众免登录；管理员可在「管理→账号管理」新建账号 |
| 数据在哪？ | 全部在本机浏览器内，断网可用、不上传任何服务器；清浏览器数据或点「重置」即清空 |
| 怎么接生产链？ | 架构上预留了"链适配层"思路（docs/04）：本平台前端与流程即产品原型，把 addBlock/校验模块替换为 FISCO BCOS/长安链 SDK 即可对接真联盟链 |

## 六、常见问题

**Q：挖矿时页面卡住了？**
A：没有卡，是在算哈希。难度 3 时可能要十几秒，属正常；演示用难度 1 或 2。

**Q：换一台电脑/手机打开，数据没了？**
A：数据存在"本机浏览器"里，换设备就是另一份数据。演示时请用同一台设备；正式版对接链后数据才会全网共享。

**Q：公众身份看不到登记按钮？**
A：正常。公众是只读角色，权限表见第二节。

**Q：玩乱了怎么办？**
A：「我的」→「重置全部数据」→ 恢复出厂（创世区块 + 示例文物）。

**Q：登录账号密码是什么？**
A：登录页下方有提示：museum/123456、exam/123456、admin/admin888；管理员可在「管理→账号管理」创建新账号。

## 七、5 分钟路演脚本（照着做）

1. 打开平台，选"馆藏单位"登录（10 秒）——"这就是我们的平台，手机浏览器就能用，双击就能演示"；
2. 「登记」里登记一件文物 → 展示挖矿动画与 4/4 节点确认（60 秒）——"每一笔登记都是真实工作量证明"；
3. 「验证」里下载档案文件 → 当场改一个字 → 上传 → 显示"被篡改"（60 秒）——"篡改 1 字节都瞒不过去"；
4. 「链」里点「校验整条链」+「模拟 1 个节点被篡改」（60 秒）——"自我体检 + 多数共识"；
5. 退出换「平台管理员」登录 → 「管理」页展示机构准入与审计日志（30 秒）——"机构治理与全程留痕"；
6. 「我的」→ 展示难度调节与重置（10 秒）——"演示环境自由可控，生产环境可平滑对接联盟链"。

## 八、配套文件关系

| 文件 | 关系 |
| --- | --- |
| 区块链演示.html | 原理入门（5 步教学） |
| 区块链平台.html | **平台本体（本手册对应文件）** |
| 存证演示demo.html | 验证功能的文物场景版 |
| 可视化总览.html | 整体架构一图看懂 |
| docs/01–06 | 技术设计文档（生产化蓝图） |




